# AI Provider API Key (required)
# This is your OpenAI API key, which can be obtained from https://platform.openai.com/
AI_PROVIDER_API_KEY=your-api-key-here

# Model Configuration
# The AI model to use (defaults to "gpt-4o" if not specified)
AI_MODEL=gpt-4o

# Optional Configuration
# Temperature controls randomness: 0 = deterministic, 1 = creative, 2 = very random
AI_TEMPERATURE=0.7

# Maximum tokens to generate (optional)
# AI_MAX_TOKENS=1000

# Public environment variables (accessible on client)
# Your website URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000 